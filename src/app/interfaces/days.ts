export interface Days {

    expanded: boolean,
    type: string,
    styleClass: string | null,
    data: {
        image: string | null,
        name: string,
        title: string
    },
    children: [ ]
}
