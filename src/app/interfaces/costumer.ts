export interface Costumer {
    id: number,
    name: string | any,
    email: string,
    phone: string | any,
    language: string | any,
    userId: number
}

export interface Representative {
    name?: string;
    image?: string;
}
